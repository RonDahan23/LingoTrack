# Base URL of the LingoTrack backend. Defaults to http://localhost:3000 when
# unset (see src/config.ts). The backend's WEB_APP_URL / CORS origin must match
# wherever this app is served (this app's dev server runs on port 5199).
VITE_API_BASE_URL=http://localhost:3000
